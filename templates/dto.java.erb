package <%= @base_package %>.<%= @model_name.downcase %>;

import java.io.Serializable;

import com.fasterxml.jackson.annotation.JsonProperty;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

/**
 * Data traonsfer object fro <%= @table_name.singular.gsub('_', ' ') %>.
 *
 * @author <%= @author %>
 *
 * <%= @license_hint %>
 */
@AllArgsConstructor
@NoArgsConstructor
@Builder
@Getter
@Setter
public class <%= @model_name %>Dto implements Serializable {
    private static final long serialVersionUID = 1L;

<%- @columns.each do |column| -%>
    <%= "@NotNull" unless column.nullable %>
    @JsonProperty("<%= column.name.camelize(false) %>")
    private <%= column.java_type %> <%= column.name.camelize(false) %>;

<%- end -%>

}
